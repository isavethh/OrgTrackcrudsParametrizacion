services:
  laravel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: org-laravel
    volumes:
      - .:/var/www
    environment:
      DB_CONNECTION: pgsql
      DB_HOST: db       # Nombre del servicio del contenedor de postgres
      DB_PORT: 5432
      DB_DATABASE: org_db
      DB_USERNAME: admin
      DB_PASSWORD: admin123
    depends_on:
      - db
    networks:
      - org-net
  nginx:
    image: nginx:latest
    container_name: orgtrack
    expose:
      - "80"
    # ports:
    #   - "8080:80" ---> despliegue local con puerto 8080
    volumes:
      - .:/var/www
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - laravel
    networks:
      - org-net
      - internal-network
      - proxy-network
  db:
    image: postgres:latest
    container_name: org-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: org_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
    volumes:
      - db-data:/var/lib/postgresql
    networks:
      - org-net

networks:
  org-net:
    driver: bridge
  internal-network:
    external: true
  proxy-network:
    external: true

volumes:
  db-data: